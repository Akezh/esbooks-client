import React, { FunctionComponent } from 'react';
import SignInView from '../views';

const SignInContainer: FunctionComponent = (): JSX.Element => {
  return <SignInView />;
};

export default SignInContainer;